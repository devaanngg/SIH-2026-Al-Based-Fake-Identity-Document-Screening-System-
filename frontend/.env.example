# Base URL of the backend API (optional - uses Next.js rewrites by default)
NEXT_PUBLIC_API_URL=

# Used by next.config.js rewrites to proxy API calls to the FastAPI backend
API_BASE_URL=http://localhost:8000
